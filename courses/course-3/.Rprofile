# Course 3 is a nested Quarto project. Use the repository-level renv project
# when rendering from this directory.
project_root <- normalizePath(file.path(getwd(), "../.."), mustWork = TRUE)
Sys.setenv(RENV_PROJECT = project_root)
source(file.path(project_root, "renv", "activate.R"))
